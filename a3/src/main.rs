// flow control using if...else 

fn main() {

    // use a variable set to true or false
    let message = false;

    // use an if...else block to determina which message to display
    if message == true{
        println!("Mesajul este adevarat");
    }else{
        println!("Mesajul este fals");
    }
    


    // use the println macro to display messages to the terminal
    

}
